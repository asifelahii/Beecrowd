#include<stdio.h>
int main()
{
    int a,b,c,max;
    scanf("%d %d %d",&a,&b,&c);
    max = a;

    if(b>a && b>c){
        max = b;
    }
    if(c>a && c>b){
        max = c;
    }

    int max2 = a;
    if(max>a && ){
        
    }



    printf("max value = %d\n",max);

    printf("\n%d\n%d\n%d\n",a,b,c);


    return 0;
}