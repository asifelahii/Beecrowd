#include<iostream>
using namespace std;
int main()
{
    int T;
    scanf("%d", &T);

    while(T--)
    {
        int n, destination;
        cin >> n >> destination;

        int gas_station[n];
        for(i=0; i<n; i++)
        {
            cin >> gas_station[i];
        }

    }
    return 0;
}